# 11 · 跨平台重构与验收（v2）

> 目标：在**没有个人目录、没有本机服务**的新机器上，clone 仓库即可安装运行。
> 分支：`cross-platform-v2`（未合并 main）。

## 一、改了什么

| 项 | 改前（Legacy） | 改后（v2） |
|---|---|---|
| 入口 | `tools/make_card.py`（macOS 本机链路） | `tools/video2draft.py`（跨平台 CLI） |
| 抖音 / B站 / YouTube 取流 | 本机解析服务 API + Node + `mcp_call.mjs` | **仓库安装的 yt-dlp**（`python -m yt_dlp`），直接拉音频 |
| 小红书 | `xhs_reply.py`（含 `/tmp` 硬编码） | `tools/adapters/xiaohongshu.py`，纯标准库 |
| X | `x_reply.py`（含个人 yt-dlp 绝对路径） | `tools/adapters/x_twitter.py`，公开接口 + yt-dlp 兜底 |
| 视频号 | 与主链路强耦合 | **独立可选适配器** `tools/adapters/shipinhao.py`，未设 `YJCG_SPH_ENDPOINT` 即关闭 |
| 出图 | `~/.local/bin/md2pic`（外部 CLI，硬编码 Chrome 路径 + 个人 venv） | 仓库自带 `tools/md2pic.py`，用**当前解释器**调用，Chrome 跨平台探测 |
| 微信推送 | 核心程序直接调 `wx-send` | 核心程序**只出 MD / PNG / JSON**，推送交给外层 |
| 目录 | `~/WorkBuddy/<时间戳>`、`~/Documents/资料库` 写死 | `Path(__file__).resolve().parent` 推导代码目录；输出走 `YJCG_OUTPUT_DIR` |
| 临时目录 | `/tmp/...` 硬编码 | `tempfile.gettempdir()`（Windows 落到 `%TEMP%`） |
| 转写 | 依赖本机 venv + 常驻服务 | 当前解释器内 `faster-whisper`（可选 mlx） |
| 环境检查 | 无 | `install.sh` / `install.ps1` + `--doctor` |
| 自动化测试 | 无 | `tests/`（11 项）+ GitHub Actions macOS/Windows |

## 二、兼容层（不破坏旧接口）

- 旧参数 `--no-push` 仍被接受（v2 CLI 本来就不推送，属于兼容占位）。
- `tools/make_card.py`、`tools/pipeline.py`、`x_reply.py`、`xhs_reply.py`、`whisper_server.py` **原样保留**，作为 macOS 本机的 Legacy 稳定版，不在本次改造范围内。
- `format_doc.load_config()` 增加环境变量来源（`DEEPSEEK_API_KEY` 等）；原来读 `tools/.sph_config.json` 的行为不变。

## 三、验收测试（Mac 实测）

| # | 用例 | 命令 | 结果 |
|---|---|---|---|
| 1 | YouTube 端到端（MD + PNG） | 见下 | 见「四、实测记录」 |
| 2 | 不带 `--no-push` / 带 `--no-push` 都不依赖 wx-send | `video2draft.py "<链接>" --no-push` | ✅ 核心程序无推送依赖，参数仅为兼容 |
| 3 | 无 DeepSeek Key | 不设 `DEEPSEEK_API_KEY` | ✅ 输出「标题 + 原始转写稿」（单元测试 `test_no_deepseek_key_keeps_raw_text`） |
| 4 | 缺 ffmpeg | `PATH=/nonexistent YJCG_FFMPEG=/nope/ffmpeg` | ✅ 中文提示含 brew/winget/apt 三种安装方式（`test_missing_ffmpeg_message_is_helpful`） |
| 5 | 出图失败仍保留 MD | mock 掉 Chrome | ✅ 返回失败但 MD 保留（`test_render_failure_keeps_md`） |
| 6 | 跨平台红线 | `python -m unittest` | ✅ 非 Legacy 文件中无个人主目录、工作区目录、外部 CLI 依赖、本机服务端口 |
| 7 | 出图冒烟（MD→PNG） | 单元测试 | ✅ 本机 Chrome 出图成功 |
| 8 | 文档链接 | 全仓链接检查脚本 | ✅ 0 条失效 |
| 9 | 个人语境 | 全文检索 | ✅ 已清除个人姓名与第三人称指代 |

## 四、实测记录

见本轮交付说明里的「实际命令 + 返回结果 + 产物路径」清单（含 Mac 端到端输出）。

## 五、还没验证的（诚实边界）

1. **Windows 端到端未实机验证**：本机没有 Windows 环境，且按约定未 push（CI 未触发）。
   已做的：代码层跨平台改造（无 `/tmp`、无 POSIX 专属调用、路径全用 `pathlib`）、
   `.github/workflows/cross-platform.yml` 含 `windows-latest` 任务、单元测试可在 Windows 跑。
   **在 CI 或真机跑通前，README 一律写「Windows 已适配、端到端待验收」**。
2. **抖音 / B站未做端到端实测**：适配器已实现（yt-dlp），本轮只实测了 YouTube。
3. **Linux 未实测**。
4. **视频号 Windows 未验证**：上游 `ltaoo/wx_channels_download` 官网自称支持 Windows/macOS，
   本项目只在 macOS 验证过，故平台矩阵里标注「macOS 已验证 / Windows 未验证」。
5. **小红书 / X 本轮未重跑真实链接**（代码从既有实现移植，逻辑未改）。
